// ... toggleStatus here

const lamp = { isOn: false } // <-- old lamp

const newLamp = toggleStatus(lamp) // <-- we send the old lamp in and new lamp comes out

console.log(`lamp is now ${lampStatus(newLamp)}`) // Lamp is now on